spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/orders_db
    username: root
    password: Sharankumar@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
